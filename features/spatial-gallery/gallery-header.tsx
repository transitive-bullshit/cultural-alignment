import { SiteHeader } from '@/components/site-header'

import styles from './gallery-page-shell.module.css'

export function GalleryHeader() {
  return <SiteHeader className={styles.galleryHeader} />
}
